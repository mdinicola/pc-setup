---
- name: Enable mise copr repository
  community.general.copr:
    name: jdxcode/mise
    state: enabled
  become: yes

- name: Install mise
  ansible.builtin.dnf:
    name: "{{ item }}"
    state: present
  become: yes
  loop:
    - mise

- name: Create mise config path
  ansible.builtin.file:
    path: ~/.config/mise
    state: directory

- name: Write global mise config file
  ansible.builtin.copy:
    src: "{{ playbook_dir }}/../../../common/mise/mise.global.toml"
    dest: ~/.config/mise/config.toml
    mode: '0644'

- name: Add mise activation to .bashrc
  ansible.builtin.lineinfile:
    path: "~/.bashrc"
    line: 'eval "$(mise activate bash)"'
    create: true
    mode: '0644'